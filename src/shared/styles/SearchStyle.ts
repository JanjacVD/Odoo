import { StyleSheet } from "react-native";

export const SearchStyle = StyleSheet.create({
    container: {
        width:'100%',
    },
    input: {
        borderWidth:1,
        borderRadius:30,
        paddingVertical:5
    }
})